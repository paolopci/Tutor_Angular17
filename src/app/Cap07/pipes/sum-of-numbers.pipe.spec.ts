import { SumOfNumbersPipe } from './sum-of-numbers.pipe';

describe('SumOfNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new SumOfNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
